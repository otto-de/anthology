# Copy this file to credentials.env, fill in real values, and NEVER commit credentials.env.
# This file is safe to commit — it contains no real secrets.
#
# Format: a single JSON object where each key is a Kafka cluster name and the value
# is a map of "username" and "password" for that Kafka cluster.
ANTHOLOGY_CREDENTIALS={"example-cluster-a":{"username":"REPLACE_ME","password":"REPLACE_ME"},"example-cluster-b":{"username":"REPLACE_ME","password":"REPLACE_ME"}}
